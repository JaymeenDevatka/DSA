#include <istream>
using namespace std;

class node
{
    int data;
    node *next;

    node(int val)
    {
        data = val;
        next = NULL;
    }
};

int main()
{
    node *head = NULL;
    head = new node(data);
    temp = head;
}